setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

build:
  docker:
    web: Dockerfile

run:
  web:
    command: ["/entrypoint.sh"]
    image: web
  worker:
    command: ["/entrypoint.sh"]
    image: web
    config:
      DYNO_TYPE: worker
