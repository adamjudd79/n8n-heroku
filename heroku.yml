setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

build:
  docker:
    web: Dockerfile

run:
  web:
    command: ["/entrypoint.sh", "start"]
    image: web
  worker:
    command: ["/entrypoint.sh", "worker"]
    image: web
